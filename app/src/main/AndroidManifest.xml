<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edulearn">

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/edu_logo"
        android:label="EduLearn"
        android:theme="@style/Theme.EduLearn">

        <activity android:name=".AttendanceActivity" />
        <activity android:name=".LiveLectureActivity" />
        <activity android:name=".RecordedLectureActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".MarksActivity" />
        <activity android:name=".HomeworkActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
